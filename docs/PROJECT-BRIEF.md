# PROJECT BRIEF — Multi-Agent Fantasy Football Research System
**Owner:** Nick | **Brief version:** 1.31 (2026-07-20) | **Update this header whenever status changes**

> **Purpose of this document:** Load this at the start of any new Claude conversation about this project. It carries the project's history, decisions, current status, and working rules so any new chat can continue without re-explaining. It is a living document — the STATUS and NEXT ACTIONS sections should be updated as work progresses; everything else changes rarely.

---

## 1. What This Project Is

A data-grounded, multi-agent AI system that produces fantasy football draft rankings (position ranks, tiers, overall value board) and a falsifiable "My Guys" list that measurably beats market consensus (ADP/ECR) for Nick's league. Success is scored in January against pre-registered criteria. The system compounds season over season: each season is archived as a new backtest world.

**Design philosophy (settled — do not relitigate in new chats):**
- Evals before agents: the backtest harness and baselines exist before anything smart is built; every design decision resolves against a scored gate, not opinion
- Tool-grounded data only: no LLM-recalled statistics anywhere; uncited claims are invalid
- Agents differ by methodology, not persona; debate exists only if it beats simple averaging on frozen worlds
- Pre-registration everywhere: theses, falsifiers, metric formulas, and price caps are written before outcomes are known
- Beat the market or ship the market: if the system can't clear ADP/ECR retroactively, draft off ADP

## 2. Current Status (UPDATE THIS SECTION)

**Phase 0 COMPLETE (2026-07-18). Phase 1 (data platform) 7/8 exit-checklist items DONE — only the 3-day capture-streak item remains, and that's pure elapsed time, not code (see §9 detail below); D-017 (2026-07-19) added a 3rd nflverse source (pbp, aggregated-only) beyond the original Phase 1 scope. Phase 2 (backtest harness) COMPLETE as of 2026-07-19 — 6/6 exit-checklist items done. Phase 3 (agent prompts) STARTED 2026-07-19: step 1 (frozen-world serving mode, D-014) DONE; step 2 (Agent 1 built + validated end-to-end at ALL FOUR positions, D-015/D-016 — M3 complete ahead of Aug 7) DONE. Headline: Agent 1 v1.0 is statistically MARKET-EQUIVALENT everywhere but 0-for-8 positive on point deltas (QB 0.193 / RB 0.573 / WR 0.609 / TE 0.222; all 8 CIs vs ADP/ECR straddle zero). O3 not cleared by this candidate. D-017 then closed the specific data gap diagnosed as driving the QB/TE weakness (red-zone/designed-run metrics), and the A1-v1.1 re-run (D-018) tested it: at TE the red-zone data produced a LARGE improvement (0.222->0.326, +0.109, first positive point estimate vs BOTH ADP and ECR at any position -- but CI [-0.007,+0.234] barely still includes zero, so promising-not-confirmed per D-013); at QB it barely moved. The full-board v1.1 (D-019) added RB/WR and confirmed the pattern: red-zone data is an ISOLATED-to-TE signal (TE +0.109; QB +0.025; RB -0.002; WR -0.001). Generalizable lesson: new data helps only where it's non-redundant with what the agent already weighs AND decision-relevant (red-zone clears both bars only at TE). Agent 1 is now fully built and characterized. Remaining: Agents 2-5, and/or confirming TE via a 2nd world (needs 2023 stats). O3 not cleared by Agent 1 alone.**

**Phase 3 step 1 recap (frozen-world serving mode, D-014):** `FROZEN_WORLD_PIN` env var flips ALL of `access/layer.py` into leakage-safe frozen-world serving (identical signatures/payloads — an agent can't tell which mode it's in); audit-gated like GOLD; live snapshot resolution hard-refused while pinned; `get_vacated_opportunity` re-derived against the archived depth chart (live version's Sleeper current-team field would be future info); per-team depth-chart Wayback dates now cited (2025 world spans Feb–Jun captures; NYJ's Feb chart still lists Davante Adams — staleness, not leakage); leakage audit extended with 6 serving-path assertions, green both worlds. Bonus: the cross-check caught a real pre-existing bug — the layer was serving REG+POST rows; now REG-only everywhere, and frozen Gibbs matches 2024 ground truth EXACTLY (21.35 PPG, 17 games). **Constraint accepted by Nick (D-014): 2023 stats stay unpulled, so Agent 1 end-to-end validation runs on the 2025-06-18 world only.**

**Phase 1 recap:** raw capture (Sleeper/FFC/ESPN) live on GitHub Actions daily cron; canonical `gsis_id` crosswalk with human-confirmed FFC name-collision overrides (`capture/ffc_confirmed_matches.json`) — unmatched queue has zero skill-position players left; full validation + GOLD marking; curated `weekly_stats` layer; all 8 agent access-layer functions (`access/layer.py`) return real data; data dictionary 11/18 metrics computable. Full detail in decisions.md D-001–D-008 and §7 items 1-13 below.

**Phase 2 recap (backtest harness, built 2026-07-18/19 — full detail in decisions.md D-009 through D-013):**
- **Ground truth** (`backtest/ground_truth.py`): actual 2024/2025 season PPG + positional rank under exact charter scoring, verified against real season leaders both years.
- **Frozen worlds re-anchored to 2024-06-18 / 2025-06-18** (D-009, supersedes D-003's original 2024-07-15/2025-07-15) — no historical ADP source exists dated exactly mid-July either year; these are the actual dates real archived market data exists for.
- **Frozen-world data** (`backtest/frozen_worlds/`): ADP and ECR both via Wayback Machine archives of FantasyPros pages, resolved to `gsis_id` via a deterministic id join (100% and 98.4%+ resolution); depth charts via Wayback archives of Ourlads' pages, full 32/32 team coverage both worlds, resolved via name-matching + reuse of the existing FFC-collision overrides. Prior-season (2023) stats and coaching staff were researched and explicitly deprioritized/deferred — not load-bearing for anything currently planned.
- **Leakage audit** (`backtest/leakage_audit.py`): all automated checks pass both worlds, including a data-grounded (not memory-based) spot-check for real market surprises.
- **Scoring engine + full Baseline Bank** (`backtest/scoring.py`, `backtest/baselines.py`): SM2 per-position Spearman correlation; ADP-order, ECR, naive-repeat (2025-06-18 world only — 2024's needs the deprioritized 2023 data), and uniform-blend all scored on both worlds (D-010/D-011/D-012).
- **Runner CLI + config hashing + trend index + dry run** (`backtest/run_backtest.py`, `backtest/trend_index.py`): the one-command interface the design calls for, verified to produce identical results to the direct baseline path.
- **Bootstrap CI** (`backtest/bootstrap_ci.py`, D-013): verified against a hand-checked synthetic example first, then applied for real — and it QUALIFIED D-011's "ECR beats ADP at QB/TE" finding as not statistically significant at these universe sizes. **Standing precedent: every future SM2/O3 comparison must report a CI alongside the point delta, never just the point delta alone.**

**Phase 3 step 2 recap (Agent 1, D-015):** `agents/` package (prompt assembly A1-v1.0 verbatim from the design doc, ADP-bounded alphabetized pools, output validation incl. a no-post-world-evidence leakage check, run storage under `backtest/agent_runs/`), registered in `run_backtest.py` (per-run stored outputs, prompt version in the config hash). 3 validated RB runs on 2025-06-18: Spearman mean 0.573 [0.562, 0.582]. vs baselines with CIs (D-013 precedent): market-equivalent, not market-beating; naive-repeat's RB 0.650 still leads everything at this position. Honest caveats recorded: same-session runs are correlated (variance is a lower bound); biggest iteration lever is the missing weighted-opportunity/red-zone data (needs nflverse pbp), not obviously the prompt.

**Phase 3 step 2 completion (D-016, M3 done):** QB/TE/WR runs completed 2026-07-19 with the same protocol — all 3 runs now cover the full 156-player charter universe. Per-position Spearman: QB 0.193, RB 0.573, WR 0.609, TE 0.222; all 8 intersection-universe bootstrap CIs vs ADP/ECR straddle zero, point deltas 0-for-8 positive. QB/TE weakness tracks the methodology's pbp-gated missing inputs (designed-run and red-zone splits).

**Data platform extension (D-017, 2026-07-19):** nflverse pbp pulled as an aggregated red-zone/rush-type summary ONLY -- full 372-column pbp (~20.7MB/season) is never persisted (repo-size deviation, flagged and justified); `capture/pull_pbp.py` writes just `curated/redzone_player_stats.parquet` + `curated/redzone_team_stats.parquet`. 3 new extension metrics (beyond the original 18): `red_zone_target_share`, `red_zone_carry_share`, `designed_run_rate`, wired into `get_player_stats()` in both live and frozen-world modes, hand-verified against pre-confirmed real facts (Saquon Barkley 64 RZ carries/6 RZ TDs 2024; Lamar Jackson 83 designed/45 scramble carries 2024, exact matches). This directly targets D-016's diagnosed QB/TE weakness. **NEXT: re-run Agent 1 as A1-v1.x with the new evidence available (at minimum QB, ideally all 4 positions) and re-score against the D-016 baseline to see if the point deltas move.**

| Phase | Design doc | Build status |
|---|---|---|
| WBS (all phases) | fantasy-ai-project-wbs.md | n/a |
| 0 Charter | phase0-charter-design.md | ✅ COMPLETE — charter.md approved; decisions.md live (D-001–D-008) |
| 1 Data platform | phase1-data-platform-design.md | ☐ 7/8 exit-checklist items DONE — raw capture, canonical-ID crosswalk (§3) incl. snap_counts and human-confirmed FFC overrides, full validation + GOLD marking (§6), the curated layer (`curated/weekly_stats`), ALL 8 agent access-layer functions (§7), and the data dictionary (§5, 11/18 metrics computable) all built and (capture side) LIVE on GitHub Actions cron. Unmatched FFC queue has zero skill-position players left. Only remaining checklist item is the 3-consecutive-day capture streak, a time-based check, not code. NOT built: nflverse pbp/participation/full-rosters/draft-combine (not currently blocking anything). |
| 2 Backtest harness | phase2-backtest-harness-design.md | ✅ COMPLETE 2026-07-19 — 6/6 exit-checklist items done (§9): ground truth, frozen-world assembly (2024-06-18/2025-06-18 per D-009, 32/32 depth-chart teams), leakage audit, Baseline Bank (D-010/D-011/D-012), runner CLI/config-hash/trend-index/dry-run, and Bootstrap CI (D-013, which qualified D-011's ECR-vs-ADP finding as not statistically significant). Coaching staff and prior-season (2023) stats researched/deprioritized, not load-bearing. |
| 3 Agents (5 prompts) | phase3-agent-prompts.md | ☐ STARTED 2026-07-19 — step 1: frozen-world serving mode (D-014). Step 2: Agent 1 validated end-to-end at ALL FOUR positions (D-015/D-016, M3 complete): QB 0.193 / RB 0.573 / WR 0.609 / TE 0.222; all 8 CIs vs ADP/ECR straddle zero. Data extension: red-zone/designed-run metrics (D-017). A1-v1.1 (D-018/D-019, full board): red-zone data is an ISOLATED-to-TE signal (TE +0.109 and now beats both baselines on point est [CI incl. zero]; QB +0.025; RB -0.002; WR -0.001). Agent 1 fully characterized. Pending: Agents 2-5, and/or 2nd-world confirmation of TE. |
| 4 Debate + judge | phase4-debate-protocol.md | ☐ Not started |
| 5 Weekly ops | phase5-ops-weekly-design.md | ☐ Not started |
| 6 Deliverables | phase6-deliverables-design.md | ☐ Not started |
| 7 Postmortem | phase7-postmortem-design.md | ☐ Runs January |

**Key dates (from charter §6):** M1 capture live Jul 24 · M2 baselines Jul 31 · M5 debate gate Aug 19 · M7 board freeze Aug 27 · Draft Aug 29/30.

## 3. Key Decisions Already Made (with rationale — do not reopen without cause)

1. **nflreadpy, not nfl_data_py** — nfl_data_py is deprecated; nflreadpy is the maintained nflverse Python library. Any doc referencing nfl_data_py is superseded on this point. (D-002)
2. **Weekly Thursday-morning analysis brief** (not daily) — matches decision tempo and prevents alert fatigue. BUT data capture (ADP/injury/depth chart snapshots) stays DAILY and silent — it is un-backfillable. Draft-week ramp: daily analysis from Aug 19.
3. **Five agents split by methodology:** opportunity/volume, efficiency (regression-mandatory), profile/comps, market/ADP, team-context (top-down). Subject to kill/merge if one adds no unique signal. (D-004)
4. **Debate: 2-round cap, agenda = top disagreements only, adjudicated by a judge on a different model/config**, with anti-bias controls (randomized order, length caps, uncited claims struck). Decision gate: debate must beat accuracy-weighted averaging on frozen worlds or it gets cut.
5. **Frozen worlds 2024-06-18 and 2025-06-18** for backtesting; each completed season becomes a new world. (D-003, dates re-anchored by D-009 2026-07-19 once research showed no historical ADP source exists dated exactly mid-July either year — these are the actual dates real archived market data exists for.)
6. **Tiers via Gaussian mixture (BIC-selected count) on projection + uncertainty**, min-gap post-processing; overall board via value-over-replacement with a VOLS-leaning, league-calibrated baseline.
7. **My Guys = mechanical selection** (board-vs-ADP delta + multi-agent agreement + surviving thesis), 5-10 players, each with pre-registered thesis, max-reach price cap, and invalidation trigger.
8. **Board freezes at draft − 2 days** (= Aug 27); post-freeze events are annotations, never regenerations.
9. **Postmortem in January** scores everything against the charter; separates decision quality from outcome quality (no "resulting"); every finding becomes a decision-log action or explicit no-action.
10. **ESPN is the league platform and PRIMARY market signal** (league 94172663, private). ESPN reader is the league-settings source of truth (diffs against charter §5) and primary ADP for DELTA/My Guys pricing; Sleeper demoted to secondary/backup ADP (satisfies risk R2); FantasyPros ECR unchanged. Auth via owner's SWID/espn_s2 cookies, local-only, never committed. Supersedes the Phase 1 design's Sleeper-as-platform assumption. (D-005)
11. **Zero API spend.** Agent/debate/backtest runs execute inside Claude Code sessions under Nick's Claude plan; the daily capture job runs on GitHub Actions free-tier cron (independent of any personal machine). Budget controls = agenda/round/word caps; capture takes priority over analysis if plan limits are hit. Supersedes the Phase 0 design's API-budget line. (D-006)
12. **Sleeper has no ADP endpoint.** Verified against Sleeper's actual API (docs.sleeper.com): it exposes player meta/injury status and trending adds/drops only. FantasyFootballCalculator's free keyless API is the ADP source instead, scoped to this league's format. Supersedes phase1-data-platform-design.md §2's "Sleeper API → ADP" line and charter §10 R2's backup-source name. (D-007)
13. **ESPN's real API host is `lm-api-reads.fantasy.espn.com`, not `fantasy.espn.com`.** The commonly-documented host now sits behind an AWS WAF JS challenge a plain HTTP client can't pass. Verified 2026-07-18 against league 94172663: `lm-api-reads` returns real data on the same `view` params, and every charter §5 field matched on the first settings-diff run. (D-008)

## 4. Success Criteria (summary — binding formulas live in phase0 doc, Appendix M)
- SM1: My Guys hit rate ≥ 60% (dual condition: beat ADP-implied finish AND startable value)
- SM2: board's rank correlation vs. actuals beats BOTH ADP and ECR, overall + ≥3 of 4 positions
- SM3: tier accuracy (±1 tier) ≥ 70%
- SM4: process — backtest gate passed pre-draft, capture ≥90% uptime Jul 24→draft, zero uncited stats in deliverables

## 5. Artifact Inventory
- Nine design docs (§2 table) — binding; deviations recorded in decisions.md, designs never edited retroactively
- **charter.md** — APPROVED 2026-07-18; §3/§4/§5 frozen (12-team ESPN, 1.0 PPR, 4pt pass TD, 7 bench, redraft snake, draft Aug 29/30)
- **decisions.md** — append-only log, D-001 through D-013 live
- Repo location: https://github.com/nreyes2112/Fantasy-Football-Agent-Test
- Data snapshot location: /data/snapshots/ in repo per phase1 design §4
- **docs/data-dictionary.md** — phase1 §5's required metric definitions; every derived metric an agent or the backtest harness will ever need is defined ONCE here, not in a prompt
**New chats: read this brief, then the charter, then the design doc for the phase being worked — the designs are binding.**

## 6. Working Rules for Any Claude Session on This Project
1. Read this brief first; then read the design doc for the phase being worked.
2. Follow the designs. Deviations are allowed but must be flagged as deviations, justified, and recorded as a decision-log entry (D-###) citing what changed.
3. Never fabricate stats, ADP values, or player data — retrieve or mark unavailable.
4. Verify anything time-sensitive (library status, API endpoints, player news) with current sources; this project has already caught one deprecated-library issue.
5. When producing build artifacts (code, configs), state which design-doc section they implement.
6. Scope guard: v1 is pre-draft season-long rankings for Nick's league only. In-season lineup tools, trade calculators, DFS, other leagues = out of scope without a charter change.
7. Keep this brief current: when a phase's status changes or a decision is made, update §2/§3 and bump the version header.

## 7. Next Actions (UPDATE THIS SECTION)
1. ~~**Claude Code:** create project folder (charter.md, decisions.md, docs/), `/init` the repo, verify/install Python~~ DONE 2026-07-18
2. ~~**Build Tier 1 daily capture — Sleeper first** (ADP, injury, trending → dated immutable snapshots per phase1 §4) + GitHub Actions cron at 06:00 ET~~ DONE 2026-07-18 — pushed to GitHub (https://github.com/nreyes2112/Fantasy-Football-Agent-Test), first end-to-end Actions run (pull → commit → push) confirmed green. ADP via FantasyFootballCalculator per D-007, not Sleeper. Well ahead of Jul 24 (M1).
3. ~~**ESPN reader:** pull league 94172663 settings with owner cookies, diff against charter §5; ESPN becomes the PRIMARY ADP source once built (D-005)~~ DONE 2026-07-18 — settings match, ESPN ADP live on the daily cron with `ESPN_SWID`/`ESPN_S2` as GitHub repo secrets.
4. ~~**Canonical-ID crosswalk (§3):** resolve Sleeper/ESPN/FFC to nflverse `gsis_id`~~ DONE 2026-07-18 — `capture/crosswalk.py` + `capture/pull_crosswalk.py`, weekly cron (`.github/workflows/weekly-crosswalk.yml`, needs a separate Python ≥3.10 venv for `nflreadpy` — see CLAUDE.md Commands). Sleeper/ESPN resolve deterministically; FFC is proposal-only per design. **STILL OPEN:** human review of the ~6 skill-position players in the unmatched queue (`data/snapshots/<date>/curated/ffc_unmatched_queue.parquet`) — 2 are genuine cross-player name collisions (Lamar Jackson QB/CB, DJ Moore WR/CB), 1 is a nickname mismatch (Kenny/Kenneth Gainwell), 3 are just outside FFC's captured ADP depth.
5. ~~**Full validation pipeline Stage 2/3 + GOLD marking (§6)**~~ DONE 2026-07-18 — `capture/validation.py`, wired into `pull_crosswalk.py` (runs post-curation, since several checks need identity resolution first). First GOLD snapshot 2026-07-18 (22/22 checks), verified locally and on GitHub Actions.
6. ~~**Read-only agent access layer (§7)**~~ DONE 2026-07-18 — `access/layer.py` + `access/snapshot_resolver.py`. All 8 functions from the design exist and are pinned to the latest GOLD snapshot (refuses non-GOLD dates). Tested against real data (Jahmyr Gibbs' ADP history, Detroit's RB depth chart, bio-similarity comps, the full ESPN scoring table). 3 of 8 (`get_player_stats`, `get_team_context`, `get_vacated_opportunity`) correctly report `available: False` pending nflverse stat pulls below — by design, not a bug.
7. ~~**nflverse player_stats pull (§2)**~~ DONE 2026-07-18 — `capture/pull_stats.py`, weekly (same workflow as the crosswalk). 2024-2025 weekly stats (145 columns incl. target_share, air_yards_share, wopr, EPA), `player_id` already the canonical gsis_id (no crosswalk join needed). `get_player_stats()` now reads this for real (season/last4/last8 windows) instead of reporting unavailable. Verified against Jahmyr Gibbs' real last-8-games stats, locally and on GitHub Actions.
8. ~~**nflverse team_stats pull (§2)**~~ DONE 2026-07-18 — `capture/pull_stats.py` extended (same script, refactored into a shared `_pull_and_validate()` helper). 2024-2025 weekly team stats (133 columns). Caught a real team-code mismatch: nflverse's `load_team_stats` uses "LA" for the Rams vs. this project's "LAR" convention everywhere else — normalized at the source. `get_team_context()` now returns real plays/game, pass rate, EPA/game — verified against Detroit and Baltimore's real 2025 profiles. PROE, Vegas win total, and OL rank remain genuinely unavailable per field (no free source for any of the three — not a pull gap, a real data-availability ceiling).
9. ~~**get_vacated_opportunity (§7, last access-layer stub)**~~ DONE 2026-07-18 — reuses player_stats + sleeper_resolved's live team field (no separate nflverse rosters pull needed): sums a departed player's prior-season target/carry volume by checking whether they're still on the team per Sleeper. Verified against real 2025→2026 departures (Denver, SF, MIN, NYJ, ATL all checked). Caught and fixed a real bug while building this: the LA/LAR (Rams) team-code fix from item 8 above had only been applied to team_stats, not player_stats — same quirk, same fix now applied to both, re-verified. **All 8 access-layer functions now return real data.**
10. ~~**Data dictionary (§5)**~~ DONE 2026-07-18 — [docs/data-dictionary.md](data-dictionary.md), all 18 required metrics defined, 10 implemented in `access/metrics.py` (aDOT, EPA_per_target, TD_rate, carry_share, draft_capital_tier, plus the flagship league-accurate PPG). **PPG hand-verified against 2 independent real stat lines exactly as the design demands** (Josh Allen 38.76/38.76, Jahmyr Gibbs 15.0/15.0). 6 metrics honestly marked not-yet-computable (5 need one more nflverse pull; `age_curve_position` is a deferred methodology decision, not a data gap).
11. ~~**nflverse snap_counts pull (§2) + snap_share (§5)**~~ DONE 2026-07-18 — `capture/sources/nflverse.py` + `capture/pull_crosswalk.py` (resolved here, not `pull_stats.py`, since it needs the crosswalk table already loaded there: this source is `pfr_player_id`-keyed, resolved via `nflverse_crosswalk.pfr_id` reusing the existing `resolve_source()` machinery). `offense_pct` IS `snap_share` directly, no derivation needed. Verified: Josh Allen 98.11% (full-time starter), Jahmyr Gibbs 67% (sensibly above his 54.98% carry_share). Data dictionary now 11/18 computable.
12. ~~**Curated layer beyond the crosswalk (§1)**~~ DONE 2026-07-18 — `capture/build_curated_stats.py` produces `curated/weekly_stats.parquet` (player_stats + snap_counts_resolved's snap_share, joined on gsis_id/season/week, zero fan-out verified). `get_player_stats()` now reads from it. **Phase 1 exit checklist (§9): 6 of 8 items done** — the remaining 2 (3-day capture streak; FFC unmatched-queue human review) are time/human-decision items, not code. See §2 above for options on what's next.
13. ~~**FFC unmatched-queue human review (§3)**~~ DONE 2026-07-18 — Nick reviewed all 6 skill-position entries; confirmed via `capture/ffc_confirmed_matches.json` (see §2 above for the specific players and reasons). Unmatched queue now has zero skill-position players. **Phase 1 exit checklist: 7 of 8 items done** — only the 3-day capture streak remains, and that's pure elapsed time.
14. ~~**Decision point:** start Phase 2 (backtest harness)~~ DONE 2026-07-18 — Nick confirmed "ground truth first" sequencing.
15. ~~**Ground-truth builder (phase2 §2, build-order step 2)**~~ DONE 2026-07-18 — `backtest/ground_truth.py`. Actual 2024/2025 REG-season PPG + positional rank under exact charter scoring (reuses `access/metrics.py`'s already-verified scoring function), games threshold 8 per charter §3, QB/RB/WR/TE only per charter §4. Stored in `backtest/ground_truth/{season}/` (outside `data/snapshots/`, per the leakage checklist's "ground truth... access denied to candidate runs"). Sanity-checked against known real leaders both seasons.
16. ~~**Source a genuinely as-of-dated historical ADP feed for the frozen worlds**~~ DONE 2026-07-18 — researched Wayback Machine archives; found FantasyPros' `ppr-overall.php` page has real snapshots at 2024-06-18/2025-06-18 (FFC's own ADP page does not, near either date). Nick chose to re-anchor the frozen worlds to these actual data dates (**D-009 supersedes D-003**) rather than mislabel 27-day-stale data as "07-15". Built `backtest/frozen_worlds/historical_adp.py` — deterministic id-join resolution to gsis_id via the crosswalk's `fantasypros_id` column (no name-matching queue needed, unlike FFC). 423/424 (2024) and 401/403 (2025) rows resolved; spot-checked top-10 both worlds against known real preseason consensus, both correct.
17. ~~**Frozen-world prior-season stats (2023) and depth charts/coaching staff**~~ RESOLVED 2026-07-19 — prior-season (2023) stats explicitly deprioritized by Nick (not pursued); depth charts DONE via Ourlads/Wayback (item 18); coaching staff researched and deferred as not load-bearing (nothing in the access layer or data dictionary consumes it yet).
18. ~~**Depth-chart component (phase2 §2)**~~ DONE 2026-07-19 — `backtest/frozen_worlds/historical_depth_charts.py`, full 32/32 team coverage both worlds via Wayback archives of Ourlads' NFL depth charts, resolved via name-matching + reuse of the FFC-collision overrides. Extensively spot-checked (Atlanta/Kansas City/Detroit/New England), including two real roster-transition facts captured correctly (Drake Maye's 2024→2025 QB2→QB1 progression; Stefon Diggs' 2025 signing to NE).
19. ~~**ECR baseline (phase2 §4)**~~ DONE 2026-07-19 (D-011) — `backtest/frozen_worlds/historical_ecr.py`, deterministic id join same as ADP, 100%/98.4% resolution. Real finding: ECR beats ADP-order at QB/TE, ADP-order holds/wins at WR — see item 22 below for why this needed qualifying.
20. ~~**Uniform-blend baseline (phase2 §4)**~~ DONE 2026-07-19 (D-012) — completes the Baseline Bank. Blending isn't a uniform win: beats every individual source at QB (both worlds) and TE (2024), but loses to the best individual source at RB/WR (2024) and TE (2025).
21. ~~**Leakage audit automation (phase2 §2, build-order step 1)**~~ DONE 2026-07-19 — `backtest/leakage_audit.py`, all automated checks pass both worlds, including a data-grounded (not memory-based) 3-player spot check for real market surprises.
22. ~~**Runner CLI + config hashing + trend index + dry run + Bootstrap CI (phase2 §5-6, build-order steps 5-6)**~~ DONE 2026-07-19 (D-013) — `backtest/run_backtest.py`, `backtest/trend_index.py`, `backtest/bootstrap_ci.py`. Bootstrap CI was verified against a hand-checked synthetic example first, then applied for real to the ECR-vs-ADP delta — and revealed item 19's "ECR beats ADP at QB/TE" point estimates are NOT statistically significant at these universe sizes. **Phase 2 exit checklist: 6/6 items done. Phase 2 is COMPLETE.**
23. ~~**Decision point: start Phase 3**~~ DONE 2026-07-19 — Nick approved Phase 3 start, confirmed bridge-first sequencing (frozen-world serving before any prompt work), and deferred the 2023 stats pull (Agent 1 validates on the 2025-06-18 world only — D-014).
24. ~~**Frozen-world access-layer serving mode (phase2 §2's identical-code-path requirement, Phase 3 step 1)**~~ DONE 2026-07-19 (D-014) — `FROZEN_WORLD_PIN` env-var pin in `access/snapshot_resolver.py` + per-function frozen branches in `access/layer.py`; audit-gated serving; leakage audit extended with a 6-assertion serving-path check (green both worlds); per-team depth-chart capture dates cited (NYJ's Feb-2025 staleness surfaced honestly); REG-only fix applied layer-wide after the cross-check caught REG+POST contamination; frozen Gibbs = ground truth exactly (21.35 PPG / 17 games).
25. ~~**Agent 1 (opportunity/volume) prompt + runner scaffolding + end-to-end RB validation**~~ DONE 2026-07-19 (D-015) — `agents/` package (prompts A1-v1.0 verbatim, ADP-bounded alphabetized pools with JAC→JAX normalization, validation incl. no-post-world-evidence check, per-run storage), registered in `run_backtest.py` with prompt version in the config hash; 3 validated 48/48 RB runs on 2025-06-18; scored + bootstrap CIs vs ADP/ECR. Result: market-equivalent at RB (deltas -0.008/-0.050, both CIs straddle zero); naive-repeat RB 0.650 still the position's best signal. Correlated-runs caveat recorded in the scorecard.
26. ~~**Agent 1 QB/WR/TE runs (finish M3)**~~ DONE 2026-07-19 (D-016) — 3 validated full-universe (156-player) runs; per-position Spearman QB 0.193 / RB 0.573 / WR 0.609 / TE 0.222; all 8 intersection-universe CIs vs ADP/ECR straddle zero, point deltas 0-for-8 positive. M3 complete ahead of Aug 7. QB/TE weakness tracks the methodology's missing inputs (designed-run + red-zone splits, both pbp-gated). One more cross-source fix: FantasyPros "JAC" → "JAX" (agents/pool.py).
27. ~~**nflverse pbp pull to close the weighted-opportunity/red-zone gap**~~ DONE 2026-07-19 (D-017) — `capture/pull_pbp.py` + `capture/sources/nflverse.py`'s `fetch_redzone_pbp_summary()`: aggregated-only (full pbp never persisted, repo-size deviation flagged), joined into `curated/weekly_stats` via `build_curated_stats.py`. 3 new metrics (`red_zone_target_share`, `red_zone_carry_share`, `designed_run_rate`) live in `access/metrics.py`, wired into `get_player_stats()` live + frozen, hand-verified exactly against Saquon Barkley and Lamar Jackson's real 2024 facts. Leakage audit still green both worlds.
28. ~~**Re-run Agent 1 as A1-v1.x with red-zone/designed-run data, re-score vs D-016**~~ DONE 2026-07-20 (D-018) — A1-v1.1 (byte-identical prompt, only D-017 data added) at QB+TE: **TE 0.222->0.326 (+0.109, CI [-0.007,+0.234], now beats both baselines on point estimate but CI includes zero); QB 0.193->0.202 (flat, CI straddles zero).** Finding: red-zone data helps the TD-dependent position it targets (TE), not QB. CI file `bootstrap_ci_agent_opportunity_v1.1_vs_baselines.json` (v1.0's preserved separately).
29. **NEXT (Nick picks): (a) full-board v1.1 — re-run RB/WR with red-zone data so a complete v1.1 scorecard exists (RB red-zone carry share for goal-line backs, WR red-zone target share); (b) begin Agents 2-5 (efficiency next per D-004) templated from the proven Agent 1 skeleton; (c) fresh-session variance runs to tighten the correlated-runs caveat.**

## 8. Project History (one paragraph)
Conceived July 2026 from Nick's idea of month-long multi-agent research with debate. Research reshaped it: static-data re-analysis adds nothing (daily → trigger-based monitoring), persona debate adds nothing (→ methodology-differentiated agents + adjudication with bias controls), and unmeasured systems can't be trusted (→ eval-first: charter metrics, frozen-world backtests, and baseline bars before any agent runs). All eight phases were designed with per-phase research during July 2026. Phase 0 executed 2026-07-18: charter approved (12-team ESPN PPR league, draft Aug 29/30), decision log seeded D-001–D-006 including two deviations (ESPN as platform/primary market; zero-API-spend execution under the Claude plan). Build begins at Phase 1 with the daily capture job.
